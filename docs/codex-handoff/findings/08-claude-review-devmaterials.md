# Claude 评 codex Phase 0/1A 开发材料（08）

评审对象：codex 提交 9bf65d91 的 `docs/dev/*` + `docs/prd/PRD-1A-最小对账闭环.md` + 对我 PRD-0 §7 的补充。

## 总评
**强，且可直接进开发。** codex 没停在 PRD，直接产出了 build-ready 的 SQLite schema DDL（含文件哈希去重的部分唯一索引、复合键索引、FK、枚举、派生规则）、PRD-1A（标准结构 + 真实数字）、验收测试（**计算出真实 fixture 的预期值**）、质量矩阵、状态机，并回答了我 PRD-0 §4 的三个待定决策（§7 默认决策合理）。

## ✅ 我核验了 codex 计算的验收锚（全部精确正确）
- **东安 IN/OUT 拆分**：IN = 常规51697.5+免疫组化24796.8+EBER238+特殊染色63+冰冻590.1+P16 15879.5 = **93264.9** ✓；OUT = HPV13172+基因14580+FISH0 = **27752.0** ✓；和 = 121016.9 ✓。归类符合边界（HPV-E6E7/基因/FISH=OUT）。
- **赣州行级跨月**：按报告日期/送检日期均得 2026-01=2570.4 / 2026-02=7534.8 / 2026-03=30114.0 / 合计 40219.2 ✓。
- 平泉 308.7×2 / 617.4、和睦家 13152 — 与既有一致。
> 结论：codex 的 TDD 锚点数字可信，可直接写红测试。

## 🔧 需要优化/补充（小而真，建议落进 PRD-1A / 验收测试再开工）
1. **行级归属月必须指定用哪个日期列**（赣州有 送检/接收/报告 三列，本样本三者同月所以巧合一致，但规则上要 pin）。建议：**结算归属默认用 `report_date`（报告出具→可结算），并在 normalized_lines 落 `row_settlement_month` 时记录所用日期列**；定稿 §5 优先级（用户选 > header > 文件名 > sheet > 行日期）补一句"行日期取 report_date 为默认"。
2. **category_summary 的分类依赖配置/目录，Phase 1A 的"固定 seed 规则"要写清**：现有 classifier 按 项目名/前缀/备注 匹配；东安科目汇总的"项目名称"是**类别名**（常规病理诊断/病理诊断HPV/基因检测…）。其中 **HPV → OUT 依赖默认目录补 HPV-E6E7（Epic G）**，否则会落 UNKNOWN。建议 PRD-1A 显式列出东安三样本所需的 seed 规则（含 HPV/基因/FISH→OUT、常规/IHC/EBER/特殊染色/冰冻/P16→IN），并标注"productize 时由逐院配置表达 category→line"。
3. **EBER(238)/特殊染色(63) 归 IN 建议向用户确认**（金额小、风险低）：EBER=原位杂交院内化学探针、特殊染色=院内实物工序 → IN 合理，但属领域判断，落测试前确认一句。
4. **新账本 ↔ 既有院级盈亏看板 的整合点要留 TODO**：`partner_month_revenue_ledger`/`out_settlement_ledger` 是新派生层，现院级盈亏看板读 `case_revenue`/`partner-pnl`。Phase 1A 只到后端通过（codex 已诚实标注前端/整合 later），但要在 PRD-1A 明确"院级盈亏看板如何消费聚合账本/OUT 台账"是 Phase 1A 之后的并表任务，避免两套收入口径并存却不对账。

## §4 决策（codex §7 默认我认可，补充）
- ABC 回填"精确优先、拒绝歧义"✓；**前置必须先跑审计**：`outbound_abc_details` 的 case_no 是否跨 partner（成本由实验室自产，可能本就全局唯一）→ 审计结果决定 T1.6 走精确复合键还是兼容单键。这个审计是 Phase 0 第一步。
- NGS 缺售价=400 / 缺成本=落库+标记+P&L排除 ✓（与定稿 §8 矩阵一致）。

## 建议
开发材料已 ready。把上面 4 点补进 PRD-1A/验收测试后，**即可启动 Phase 0 编码**（先跑 ABC case_no 跨院审计 → lis_cases 迁移红测试 → 全链路复合键 → 守黄金）。不建议再为路线图/PRD 多轮纯文档互评。
