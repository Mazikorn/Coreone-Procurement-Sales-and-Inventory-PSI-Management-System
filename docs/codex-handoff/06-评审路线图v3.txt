# 角色与任务
你是资深产品负责人 + 病理实验室财务/对账领域专家 + 数据架构师。评审产品路线图 **v3**，目标是**尽量少错失细节、把异常/未覆盖功能挖全**。不是挑文字，是找"上线后会卡住财务、会算错账、会被漏掉"的真实场景。

# 必读
- 路线图 v3：docs/COREONE-配置驱动导入器-产品路线图-2026-06-29.md（重点 §5 规范行数据模型、§6 状态机、§7 质量标记、§10 异常与边界全清单、§12 分阶段）
- 真实对账单样本：后端代码/server/tests/fixtures/statements/*.json（7 模板家族）
- 背景：docs/codex-handoff/产品目的与素材说明.md
位置：克隆本仓库 → git checkout codex-rereview-p0-p6 → 仓库根目录。

# 重点评审（逐条给结论）
1. **§10 异常清单还漏了什么？** 对照 7 个真实样本逐个想：还有哪些文件/病例/金额/分类/成本/关账/权限/跨系统层的异常没被列到？尤其是会让财务"关不了账"或"算错"的。给**补充的异常条目**（按 §10 的层分类）。
2. **§5 规范行 `statement_normalized_lines` 字段够不够？** 7 类模板（逐病例/科目汇总/宽表远程/宽表诊断费/服务费混合/纯外送/共建）的金额行都能无损落进这张表吗？缺哪些字段？`line_grain`/`amount_role`/`business_line` 的取值枚举够不够？聚合行(无 case_no)、retainer(月度一次性)、红冲/调整行 怎么表达？
3. **§6 状态机 + §7 质量标记** 覆盖真实月结流程吗？有没有状态/标记缺失（如"已导入但模板未识别""LIS 待匹配但已先入收入""成本晚到触发重算中"）？质量标记的 blocks_posting/blocks_closing 划分对吗？
4. **§12 Phase 1A 最小可关账月** 用东安/赣州/平泉 3 样本真能关一个账吗？端到端哪一步会卡？最小差异包应包含什么字段才算"可对账"？
5. **决策一致性**：§4 的 5 个决策（复合键/先修正确性/共建逐院/已结账调整单/对账单主源）有没有内部矛盾或被 §8–§14 某处违反？

# 输出
按 **必须补（会卡住月结/算错账）/ 建议补（重要但不阻断）/ 可选增强** 三档。每条：缺什么 + 为什么是问题（结合真实样本）+ 落到 v3 哪一节/哪张表/哪个状态。先一句话总体判断（v3 是否已可作为工程排期基线、还差哪类覆盖）。中文。
（findings 写到 docs/codex-handoff/findings/06-roadmap-v3.md，commit 回 codex-rereview-p0-p6 并 push。）
