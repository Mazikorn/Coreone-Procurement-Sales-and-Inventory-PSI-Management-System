# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: scenarios\warehouse-manager-suite\stocktaking.spec.ts >> 仓管员盘点 - 盘点后查看结果路径 >> 路径6-步骤6: 提交盘点成功
- Location: e2e\scenarios\warehouse-manager-suite\stocktaking.spec.ts:720:3

# Error details

```
Error: expect(received).toContain(expected) // indexOf

Expected value: 200
Received array: [201, 400]
```

# Test source

```ts
  632 |       await materialSelect.selectOption({ index: 1 })
  633 |     }
  634 |   })
  635 | 
  636 |   test('路径5-步骤5: 填写实际库存（负数）失败', async ({ page }) => {
  637 |     if (!materialId) {
  638 |       test.skip()
  639 |       return
  640 |     }
  641 | 
  642 |     const res = await apiFetch(warehouseManagerToken, 'POST', '/stocktaking', {
  643 |       materialId,
  644 |       actualStock: -5,
  645 |       remark: 'E2E负数测试',
  646 |     })
  647 | 
  648 |     // 应该返回400
  649 |     expect([400, 201]).toContain(res.status)
  650 |   })
  651 | 
  652 |   test('路径5-步骤6: 提示用户数量无效', async ({ page }) => {
  653 |     await loginAs(page, 'warehouse_manager')
  654 |     await page.goto(`${FE_BASE}/stocktaking`)
  655 |     await page.waitForTimeout(1000)
  656 | 
  657 |     // 验证页面不会崩溃
  658 |     await expect(page.locator('body')).toBeVisible()
  659 |   })
  660 | })
  661 | 
  662 | // ────────────────────────────────────────────
  663 | // 路径6: 盘点后查看结果路径
  664 | // ────────────────────────────────────────────
  665 | 
  666 | test.describe('仓管员盘点 - 盘点后查看结果路径', () => {
  667 |   test('路径6-步骤1: 仓管员登录成功', async ({ page }) => {
  668 |     await loginAs(page, 'warehouse_manager')
  669 |     await expect(page).toHaveURL(`${FE_BASE}/`)
  670 |   })
  671 | 
  672 |   test('路径6-步骤2: 进入盘点页面成功', async ({ page }) => {
  673 |     await loginAs(page, 'warehouse_manager')
  674 |     await page.goto(`${FE_BASE}/stocktaking`)
  675 |     await expect(page.locator('body')).toBeVisible({ timeout: 30000 })
  676 |   })
  677 | 
  678 |   test('路径6-步骤3: 点击新建盘点按钮成功', async ({ page }) => {
  679 |     await loginAs(page, 'warehouse_manager')
  680 |     await page.goto(`${FE_BASE}/stocktaking`)
  681 |     await page.waitForTimeout(1000)
  682 | 
  683 |     const addButton = page.locator('button:has-text("新建盘点"), button:has-text("新增")')
  684 |     await expect(addButton.first()).toBeVisible({ timeout: 5000 })
  685 |   })
  686 | 
  687 |   test('路径6-步骤4: 选择物料成功', async ({ page }) => {
  688 |     await loginAs(page, 'warehouse_manager')
  689 |     await page.goto(`${FE_BASE}/stocktaking`)
  690 |     await page.waitForTimeout(1000)
  691 | 
  692 |     const addButton = page.locator('button:has-text("新建盘点"), button:has-text("新增")')
  693 |     await addButton.first().click()
  694 | 
  695 |     const materialSelect = page.locator('select[name="materialId"], [data-testid="material-select"]')
  696 |     if (await materialSelect.isVisible().catch(() => false)) {
  697 |       await materialSelect.selectOption({ index: 1 })
  698 |     }
  699 |   })
  700 | 
  701 |   test('路径6-步骤5: 填写实际库存成功', async ({ page }) => {
  702 |     await loginAs(page, 'warehouse_manager')
  703 |     await page.goto(`${FE_BASE}/stocktaking`)
  704 |     await page.waitForTimeout(1000)
  705 | 
  706 |     const addButton = page.locator('button:has-text("新建盘点"), button:has-text("新增")')
  707 |     await addButton.first().click()
  708 | 
  709 |     const materialSelect = page.locator('select[name="materialId"], [data-testid="material-select"]')
  710 |     if (await materialSelect.isVisible().catch(() => false)) {
  711 |       await materialSelect.selectOption({ index: 1 })
  712 |     }
  713 | 
  714 |     const actualStockInput = page.locator('input[name="actualStock"]')
  715 |     if (await actualStockInput.isVisible().catch(() => false)) {
  716 |       await actualStockInput.fill('100')
  717 |     }
  718 |   })
  719 | 
  720 |   test('路径6-步骤6: 提交盘点成功', async ({ page }) => {
  721 |     if (!materialId) {
  722 |       test.skip()
  723 |       return
  724 |     }
  725 | 
  726 |     const res = await apiFetch(warehouseManagerToken, 'POST', '/stocktaking', {
  727 |       materialId,
  728 |       actualStock: 100,
  729 |       remark: 'E2E查看结果测试',
  730 |     })
  731 | 
> 732 |     expect([201, 400]).toContain(res.status)
      |                        ^ Error: expect(received).toContain(expected) // indexOf
  733 |   })
  734 | 
  735 |   test('路径6-步骤7: 确认差异成功', async ({ page }) => {
  736 |     if (!materialId) {
  737 |       test.skip()
  738 |       return
  739 |     }
  740 | 
  741 |     const createRes = await apiFetch(warehouseManagerToken, 'POST', '/stocktaking', {
  742 |       materialId,
  743 |       actualStock: 100,
  744 |       remark: 'E2E查看结果测试-确认差异',
  745 |     })
  746 | 
  747 |     if (createRes.status !== 201) {
  748 |       test.skip()
  749 |       return
  750 |     }
  751 | 
  752 |     const stocktakingId = createRes.data?.data?.id
  753 |     if (!stocktakingId) {
  754 |       test.skip()
  755 |       return
  756 |     }
  757 | 
  758 |     const confirmRes = await apiFetch(warehouseManagerToken, 'POST', `/stocktaking/${stocktakingId}/confirm`)
  759 |     expect([200, 400]).toContain(confirmRes.status)
  760 |   })
  761 | 
  762 |   test('路径6-步骤8: 查看盘点记录', async ({ page }) => {
  763 |     await loginAs(page, 'warehouse_manager')
  764 |     await page.goto(`${FE_BASE}/stocktaking`)
  765 |     await page.waitForTimeout(1000)
  766 | 
  767 |     const rows = page.locator('table tbody tr')
  768 |     await expect(rows.first()).toBeVisible({ timeout: 5000 })
  769 |   })
  770 | 
  771 |   test('路径6-步骤9: 查看盘点详情', async ({ page }) => {
  772 |     await loginAs(page, 'warehouse_manager')
  773 |     await page.goto(`${FE_BASE}/stocktaking`)
  774 |     await page.waitForTimeout(1000)
  775 | 
  776 |     const firstRow = page.locator('table tbody tr').first()
  777 |     if (await firstRow.isVisible().catch(() => false)) {
  778 |       await firstRow.click()
  779 | 
  780 |       await expect(page.locator('text=盘点详情, text=物料信息')).toBeVisible({ timeout: 5000 })
  781 |     }
  782 |   })
  783 | 
  784 |   test('路径6-步骤10: 查看库存日志', async ({ page }) => {
  785 |     await loginAs(page, 'warehouse_manager')
  786 |     await page.goto(`${FE_BASE}/logs`)
  787 |     await page.waitForTimeout(1000)
  788 | 
  789 |     await expect(page.locator('body')).toBeVisible()
  790 |   })
  791 | 
  792 |   test('路径6-步骤11: 生成差异报告', async ({ page }) => {
  793 |     await loginAs(page, 'warehouse_manager')
  794 |     await page.goto(`${FE_BASE}/stocktaking`)
  795 |     await page.waitForTimeout(1000)
  796 | 
  797 |     // 验证页面不会崩溃
  798 |     await expect(page.locator('body')).toBeVisible()
  799 |   })
  800 | })
  801 | 
```