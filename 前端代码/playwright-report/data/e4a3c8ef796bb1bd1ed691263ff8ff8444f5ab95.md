# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: scenarios\technician-suite\project-outbound.spec.ts >> 技术员项目领用出库 - 出库失败（库存不足）路径 >> 路径2-步骤8: 提交出库失败（库存不足）
- Location: e2e\scenarios\technician-suite\project-outbound.spec.ts:367:3

# Error details

```
Error: expect(received).toContain(expected) // indexOf

Expected value: 403
Received array: [422, 201]
```

# Test source

```ts
  281 |     await loginAs(page, 'technician')
  282 |     await page.goto(`${FE_BASE}/outbound`)
  283 |     await page.waitForTimeout(1000)
  284 | 
  285 |     const addButton = page.locator('button:has-text("新增出库"), button:has-text("新增")')
  286 |     await expect(addButton.first()).toBeVisible({ timeout: 5000 })
  287 |   })
  288 | 
  289 |   test('路径2-步骤4: 选择项目领用类型', async ({ page }) => {
  290 |     await loginAs(page, 'technician')
  291 |     await page.goto(`${FE_BASE}/outbound`)
  292 |     await page.waitForTimeout(1000)
  293 | 
  294 |     const addButton = page.locator('button:has-text("新增出库"), button:has-text("新增")')
  295 |     await addButton.first().click()
  296 | 
  297 |     const projectType = page.locator('text=项目领用, [value="project"]')
  298 |     await expect(projectType.first()).toBeVisible({ timeout: 5000 })
  299 |   })
  300 | 
  301 |   test('路径2-步骤5: 选择项目成功', async ({ page }) => {
  302 |     await loginAs(page, 'technician')
  303 |     await page.goto(`${FE_BASE}/outbound`)
  304 |     await page.waitForTimeout(1000)
  305 | 
  306 |     const addButton = page.locator('button:has-text("新增出库"), button:has-text("新增")')
  307 |     await addButton.first().click()
  308 | 
  309 |     const projectType = page.locator('text=项目领用, [value="project"]')
  310 |     await projectType.first().click()
  311 | 
  312 |     const projectSelect = page.locator('select[name="projectId"], [data-testid="project-select"]')
  313 |     if (await projectSelect.isVisible().catch(() => false)) {
  314 |       await projectSelect.selectOption({ index: 1 })
  315 |     }
  316 |   })
  317 | 
  318 |   test('路径2-步骤6: 选择BOM成功', async ({ page }) => {
  319 |     await loginAs(page, 'technician')
  320 |     await page.goto(`${FE_BASE}/outbound`)
  321 |     await page.waitForTimeout(1000)
  322 | 
  323 |     const addButton = page.locator('button:has-text("新增出库"), button:has-text("新增")')
  324 |     await addButton.first().click()
  325 | 
  326 |     const projectType = page.locator('text=项目领用, [value="project"]')
  327 |     await projectType.first().click()
  328 | 
  329 |     const projectSelect = page.locator('select[name="projectId"], [data-testid="project-select"]')
  330 |     if (await projectSelect.isVisible().catch(() => false)) {
  331 |       await projectSelect.selectOption({ index: 1 })
  332 |     }
  333 | 
  334 |     const bomSelect = page.locator('select[name="bomId"], [data-testid="bom-select"]')
  335 |     if (await bomSelect.isVisible().catch(() => false)) {
  336 |       await bomSelect.selectOption({ index: 1 })
  337 |     }
  338 |   })
  339 | 
  340 |   test('路径2-步骤7: 填写出库数量成功', async ({ page }) => {
  341 |     await loginAs(page, 'technician')
  342 |     await page.goto(`${FE_BASE}/outbound`)
  343 |     await page.waitForTimeout(1000)
  344 | 
  345 |     const addButton = page.locator('button:has-text("新增出库"), button:has-text("新增")')
  346 |     await addButton.first().click()
  347 | 
  348 |     const projectType = page.locator('text=项目领用, [value="project"]')
  349 |     await projectType.first().click()
  350 | 
  351 |     const projectSelect = page.locator('select[name="projectId"], [data-testid="project-select"]')
  352 |     if (await projectSelect.isVisible().catch(() => false)) {
  353 |       await projectSelect.selectOption({ index: 1 })
  354 |     }
  355 | 
  356 |     const bomSelect = page.locator('select[name="bomId"], [data-testid="bom-select"]')
  357 |     if (await bomSelect.isVisible().catch(() => false)) {
  358 |       await bomSelect.selectOption({ index: 1 })
  359 |     }
  360 | 
  361 |     const quantityInput = page.locator('input[name="quantity"], input[name="sampleCount"]')
  362 |     if (await quantityInput.isVisible().catch(() => false)) {
  363 |       await quantityInput.fill('999999')
  364 |     }
  365 |   })
  366 | 
  367 |   test('路径2-步骤8: 提交出库失败（库存不足）', async ({ page }) => {
  368 |     if (!materialId || !projectId || !bomId) {
  369 |       test.skip()
  370 |       return
  371 |     }
  372 | 
  373 |     const res = await apiFetch(technicianToken, 'POST', '/outbound', {
  374 |       type: 'project',
  375 |       projectId,
  376 |       items: [{ materialId, quantity: 999999 }],
  377 |       remark: 'E2E库存不足测试',
  378 |     })
  379 | 
  380 |     // 应该返回422（库存不足）
> 381 |     expect([422, 201]).toContain(res.status)
      |                        ^ Error: expect(received).toContain(expected) // indexOf
  382 |   })
  383 | 
  384 |   test('路径2-步骤9: 提示用户库存不足', async ({ page }) => {
  385 |     await loginAs(page, 'technician')
  386 |     await page.goto(`${FE_BASE}/outbound`)
  387 |     await page.waitForTimeout(1000)
  388 | 
  389 |     // 验证页面上有错误提示
  390 |     const errorToast = page.locator('text=库存不足, text=数量不足, text=insufficient')
  391 |     // 注意：这个断言可能会失败，因为错误提示可能不会显示
  392 |     // 这里主要是验证页面不会崩溃
  393 |     await expect(page.locator('body')).toBeVisible()
  394 |   })
  395 | })
  396 | 
  397 | // ────────────────────────────────────────────
  398 | // 路径3: 出库失败（网络错误）路径
  399 | // ────────────────────────────────────────────
  400 | 
  401 | test.describe('技术员项目领用出库 - 出库失败（网络错误）路径', () => {
  402 |   test('路径3-步骤1: 技术员登录成功', async ({ page }) => {
  403 |     await loginAs(page, 'technician')
  404 |     await expect(page).toHaveURL(`${FE_BASE}/`)
  405 |   })
  406 | 
  407 |   test('路径3-步骤2: 进入出库页面成功', async ({ page }) => {
  408 |     await loginAs(page, 'technician')
  409 |     await page.goto(`${FE_BASE}/outbound`)
  410 |     await expect(page.locator('body')).toBeVisible({ timeout: 30000 })
  411 |   })
  412 | 
  413 |   test('路径3-步骤3: 点击新增出库按钮成功', async ({ page }) => {
  414 |     await loginAs(page, 'technician')
  415 |     await page.goto(`${FE_BASE}/outbound`)
  416 |     await page.waitForTimeout(1000)
  417 | 
  418 |     const addButton = page.locator('button:has-text("新增出库"), button:has-text("新增")')
  419 |     await expect(addButton.first()).toBeVisible({ timeout: 5000 })
  420 |   })
  421 | 
  422 |   test('路径3-步骤4: 选择项目领用类型', async ({ page }) => {
  423 |     await loginAs(page, 'technician')
  424 |     await page.goto(`${FE_BASE}/outbound`)
  425 |     await page.waitForTimeout(1000)
  426 | 
  427 |     const addButton = page.locator('button:has-text("新增出库"), button:has-text("新增")')
  428 |     await addButton.first().click()
  429 | 
  430 |     const projectType = page.locator('text=项目领用, [value="project"]')
  431 |     await expect(projectType.first()).toBeVisible({ timeout: 5000 })
  432 |   })
  433 | 
  434 |   test('路径3-步骤5: 选择项目成功', async ({ page }) => {
  435 |     await loginAs(page, 'technician')
  436 |     await page.goto(`${FE_BASE}/outbound`)
  437 |     await page.waitForTimeout(1000)
  438 | 
  439 |     const addButton = page.locator('button:has-text("新增出库"), button:has-text("新增")')
  440 |     await addButton.first().click()
  441 | 
  442 |     const projectType = page.locator('text=项目领用, [value="project"]')
  443 |     await projectType.first().click()
  444 | 
  445 |     const projectSelect = page.locator('select[name="projectId"], [data-testid="project-select"]')
  446 |     if (await projectSelect.isVisible().catch(() => false)) {
  447 |       await projectSelect.selectOption({ index: 1 })
  448 |     }
  449 |   })
  450 | 
  451 |   test('路径3-步骤6: 选择BOM成功', async ({ page }) => {
  452 |     await loginAs(page, 'technician')
  453 |     await page.goto(`${FE_BASE}/outbound`)
  454 |     await page.waitForTimeout(1000)
  455 | 
  456 |     const addButton = page.locator('button:has-text("新增出库"), button:has-text("新增")')
  457 |     await addButton.first().click()
  458 | 
  459 |     const projectType = page.locator('text=项目领用, [value="project"]')
  460 |     await projectType.first().click()
  461 | 
  462 |     const projectSelect = page.locator('select[name="projectId"], [data-testid="project-select"]')
  463 |     if (await projectSelect.isVisible().catch(() => false)) {
  464 |       await projectSelect.selectOption({ index: 1 })
  465 |     }
  466 | 
  467 |     const bomSelect = page.locator('select[name="bomId"], [data-testid="bom-select"]')
  468 |     if (await bomSelect.isVisible().catch(() => false)) {
  469 |       await bomSelect.selectOption({ index: 1 })
  470 |     }
  471 |   })
  472 | 
  473 |   test('路径3-步骤7: 填写出库数量成功', async ({ page }) => {
  474 |     await loginAs(page, 'technician')
  475 |     await page.goto(`${FE_BASE}/outbound`)
  476 |     await page.waitForTimeout(1000)
  477 | 
  478 |     const addButton = page.locator('button:has-text("新增出库"), button:has-text("新增")')
  479 |     await addButton.first().click()
  480 | 
  481 |     const projectType = page.locator('text=项目领用, [value="project"]')
```