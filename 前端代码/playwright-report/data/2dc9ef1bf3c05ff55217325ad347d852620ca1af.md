# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: scenarios\technician-suite\view-inventory.spec.ts >> 技术员查看库存 - 库存数量验证路径 >> 路径2-步骤7: 验证API返回库存数据格式
- Location: e2e\scenarios\technician-suite\view-inventory.spec.ts:180:3

# Error details

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: 200
Received: 401
```

# Test source

```ts
  83  |   test('路径1-步骤5: 验证物料名称列可见', async ({ page }) => {
  84  |     if (!materialId) {
  85  |       test.skip()
  86  |       return
  87  |     }
  88  | 
  89  |     await loginAs(page, 'technician')
  90  |     await page.goto(`${FE_BASE}/inventory`)
  91  |     await page.waitForTimeout(1000)
  92  | 
  93  |     // 验证物料名称列存在
  94  |     const nameColumn = page.locator('th:has-text("物料名称"), th:has-text("名称"), th:has-text("Material Name"), th:has-text("Name")')
  95  |     await expect(nameColumn.first()).toBeVisible({ timeout: 5000 })
  96  |   })
  97  | 
  98  |   test('路径1-步骤6: 验证库存数量列可见', async ({ page }) => {
  99  |     if (!materialId) {
  100 |       test.skip()
  101 |       return
  102 |     }
  103 | 
  104 |     await loginAs(page, 'technician')
  105 |     await page.goto(`${FE_BASE}/inventory`)
  106 |     await page.waitForTimeout(1000)
  107 | 
  108 |     // 验证库存数量列存在
  109 |     const stockColumn = page.locator('th:has-text("库存数量"), th:has-text("库存"), th:has-text("Stock"), th:has-text("Quantity")')
  110 |     await expect(stockColumn.first()).toBeVisible({ timeout: 5000 })
  111 |   })
  112 | 
  113 |   test('路径1-步骤7: 验证API返回库存列表', async ({ page }) => {
  114 |     const res = await apiFetch(technicianToken, 'GET', '/inventory?page=1&pageSize=10')
  115 | 
  116 |     expect(res.status).toBe(200)
  117 |     expect(res.data?.data?.list).toBeDefined()
  118 |   })
  119 | })
  120 | 
  121 | // ────────────────────────────────────────────
  122 | // 路径2: 查看库存 → 验证库存数量正确（API检查）
  123 | // ────────────────────────────────────────────
  124 | 
  125 | test.describe('技术员查看库存 - 库存数量验证路径', () => {
  126 |   test('路径2-步骤1: 技术员登录成功', async ({ page }) => {
  127 |     await loginAs(page, 'technician')
  128 |     await expect(page).toHaveURL(`${FE_BASE}/`)
  129 |   })
  130 | 
  131 |   test('路径2-步骤2: 进入库存页面成功', async ({ page }) => {
  132 |     await loginAs(page, 'technician')
  133 |     await page.goto(`${FE_BASE}/inventory`)
  134 |     await expect(page.locator('body')).toBeVisible({ timeout: 30000 })
  135 |   })
  136 | 
  137 |   test('路径2-步骤3: 验证库存列表加载', async ({ page }) => {
  138 |     await loginAs(page, 'technician')
  139 |     await page.goto(`${FE_BASE}/inventory`)
  140 |     await page.waitForTimeout(1000)
  141 | 
  142 |     await expect(page.locator('body')).toBeVisible()
  143 |   })
  144 | 
  145 |   test('路径2-步骤4: API获取库存数量', async ({ page }) => {
  146 |     if (!materialId) {
  147 |       test.skip()
  148 |       return
  149 |     }
  150 | 
  151 |     const stock = await getStock(technicianToken, materialId)
  152 |     expect(stock).toBeGreaterThanOrEqual(0)
  153 |   })
  154 | 
  155 |   test('路径2-步骤5: 验证库存数量为非负数', async ({ page }) => {
  156 |     if (!materialId) {
  157 |       test.skip()
  158 |       return
  159 |     }
  160 | 
  161 |     const stock = await getStock(technicianToken, materialId)
  162 |     expect(stock).toBeGreaterThanOrEqual(0)
  163 |   })
  164 | 
  165 |   test('路径2-步骤6: 验证库存数量在UI中显示', async ({ page }) => {
  166 |     if (!materialId) {
  167 |       test.skip()
  168 |       return
  169 |     }
  170 | 
  171 |     await loginAs(page, 'technician')
  172 |     await page.goto(`${FE_BASE}/inventory`)
  173 |     await page.waitForTimeout(1000)
  174 | 
  175 |     // 验证库存数量在页面中显示
  176 |     const stockValue = page.locator('text=/\\d+/')
  177 |     await expect(stockValue.first()).toBeVisible({ timeout: 5000 })
  178 |   })
  179 | 
  180 |   test('路径2-步骤7: 验证API返回库存数据格式', async ({ page }) => {
  181 |     const res = await apiFetch(technicianToken, 'GET', '/inventory?page=1&pageSize=10')
  182 | 
> 183 |     expect(res.status).toBe(200)
      |                        ^ Error: expect(received).toBe(expected) // Object.is equality
  184 |     expect(res.data?.data?.list).toBeDefined()
  185 | 
  186 |     // 验证库存数据格式
  187 |     if (res.data?.data?.list?.length > 0) {
  188 |       const item = res.data.data.list[0]
  189 |       expect(item).toHaveProperty('stock')
  190 |     }
  191 |   })
  192 | })
  193 | 
  194 | // ────────────────────────────────────────────
  195 | // 路径3: 查看库存 → 按物料名称筛选
  196 | // ────────────────────────────────────────────
  197 | 
  198 | test.describe('技术员查看库存 - 按物料名称筛选路径', () => {
  199 |   test('路径3-步骤1: 技术员登录成功', async ({ page }) => {
  200 |     await loginAs(page, 'technician')
  201 |     await expect(page).toHaveURL(`${FE_BASE}/`)
  202 |   })
  203 | 
  204 |   test('路径3-步骤2: 进入库存页面成功', async ({ page }) => {
  205 |     await loginAs(page, 'technician')
  206 |     await page.goto(`${FE_BASE}/inventory`)
  207 |     await expect(page.locator('body')).toBeVisible({ timeout: 30000 })
  208 |   })
  209 | 
  210 |   test('路径3-步骤3: 验证搜索框可见', async ({ page }) => {
  211 |     await loginAs(page, 'technician')
  212 |     await page.goto(`${FE_BASE}/inventory`)
  213 |     await page.waitForTimeout(1000)
  214 | 
  215 |     // 验证搜索框存在
  216 |     const searchInput = page.locator('input[type="search"], input[placeholder*="搜索"], input[placeholder*="Search"], input[name="search"], input[name="keyword"]')
  217 |     await expect(searchInput.first()).toBeVisible({ timeout: 5000 })
  218 |   })
  219 | 
  220 |   test('路径3-步骤4: 输入物料名称搜索', async ({ page }) => {
  221 |     await loginAs(page, 'technician')
  222 |     await page.goto(`${FE_BASE}/inventory`)
  223 |     await page.waitForTimeout(1000)
  224 | 
  225 |     // 输入搜索关键词
  226 |     const searchInput = page.locator('input[type="search"], input[placeholder*="搜索"], input[placeholder*="Search"], input[name="search"], input[name="keyword"]')
  227 |     if (await searchInput.first().isVisible().catch(() => false)) {
  228 |       await searchInput.first().fill('物料')
  229 |     }
  230 |   })
  231 | 
  232 |   test('路径3-步骤5: 点击搜索按钮', async ({ page }) => {
  233 |     await loginAs(page, 'technician')
  234 |     await page.goto(`${FE_BASE}/inventory`)
  235 |     await page.waitForTimeout(1000)
  236 | 
  237 |     // 输入搜索关键词
  238 |     const searchInput = page.locator('input[type="search"], input[placeholder*="搜索"], input[placeholder*="Search"], input[name="search"], input[name="keyword"]')
  239 |     if (await searchInput.first().isVisible().catch(() => false)) {
  240 |       await searchInput.first().fill('物料')
  241 |     }
  242 | 
  243 |     // 点击搜索按钮
  244 |     const searchButton = page.locator('button:has-text("搜索"), button:has-text("查询"), button:has-text("Search"), button[type="submit"]')
  245 |     if (await searchButton.first().isVisible().catch(() => false)) {
  246 |       await searchButton.first().click()
  247 |     }
  248 |   })
  249 | 
  250 |   test('路径3-步骤6: 验证筛选结果', async ({ page }) => {
  251 |     await loginAs(page, 'technician')
  252 |     await page.goto(`${FE_BASE}/inventory`)
  253 |     await page.waitForTimeout(1000)
  254 | 
  255 |     // 验证页面不会崩溃
  256 |     await expect(page.locator('body')).toBeVisible()
  257 |   })
  258 | 
  259 |   test('路径3-步骤7: 验证API支持物料名称筛选', async ({ page }) => {
  260 |     const res = await apiFetch(technicianToken, 'GET', '/inventory?page=1&pageSize=10&keyword=物料')
  261 | 
  262 |     expect(res.status).toBe(200)
  263 |   })
  264 | })
  265 | 
  266 | // ────────────────────────────────────────────
  267 | // 路径4: 查看库存 → 验证库存水平指示器
  268 | // ────────────────────────────────────────────
  269 | 
  270 | test.describe('技术员查看库存 - 库存水平指示器路径', () => {
  271 |   test('路径4-步骤1: 技术员登录成功', async ({ page }) => {
  272 |     await loginAs(page, 'technician')
  273 |     await expect(page).toHaveURL(`${FE_BASE}/`)
  274 |   })
  275 | 
  276 |   test('路径4-步骤2: 进入库存页面成功', async ({ page }) => {
  277 |     await loginAs(page, 'technician')
  278 |     await page.goto(`${FE_BASE}/inventory`)
  279 |     await expect(page.locator('body')).toBeVisible({ timeout: 30000 })
  280 |   })
  281 | 
  282 |   test('路径4-步骤3: 验证库存列表加载', async ({ page }) => {
  283 |     await loginAs(page, 'technician')
```