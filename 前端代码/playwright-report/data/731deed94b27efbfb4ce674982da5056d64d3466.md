# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: scenarios\warehouse-manager-suite\stocktaking.spec.ts >> 仓管员盘点 - 无差异成功路径 >> 路径3-步骤7: 提交盘点成功
- Location: e2e\scenarios\warehouse-manager-suite\stocktaking.spec.ts:464:3

# Error details

```
Error: expect(received).toContain(expected) // indexOf

Expected value: 200
Received array: [201, 400]
```

# Test source

```ts
  378 |   })
  379 | 
  380 |   test('路径2-步骤12: 验证库存日志正确', async ({ page }) => {
  381 |     await loginAs(page, 'warehouse_manager')
  382 |     await page.goto(`${FE_BASE}/logs`)
  383 |     await page.waitForTimeout(1000)
  384 | 
  385 |     await expect(page.locator('body')).toBeVisible()
  386 |   })
  387 | })
  388 | 
  389 | // ────────────────────────────────────────────
  390 | // 路径3: 无差异成功路径
  391 | // ────────────────────────────────────────────
  392 | 
  393 | test.describe('仓管员盘点 - 无差异成功路径', () => {
  394 |   test('路径3-步骤1: 仓管员登录成功', async ({ page }) => {
  395 |     await loginAs(page, 'warehouse_manager')
  396 |     await expect(page).toHaveURL(`${FE_BASE}/`)
  397 |   })
  398 | 
  399 |   test('路径3-步骤2: 进入盘点页面成功', async ({ page }) => {
  400 |     await loginAs(page, 'warehouse_manager')
  401 |     await page.goto(`${FE_BASE}/stocktaking`)
  402 |     await expect(page.locator('body')).toBeVisible({ timeout: 30000 })
  403 |   })
  404 | 
  405 |   test('路径3-步骤3: 点击新建盘点按钮成功', async ({ page }) => {
  406 |     await loginAs(page, 'warehouse_manager')
  407 |     await page.goto(`${FE_BASE}/stocktaking`)
  408 |     await page.waitForTimeout(1000)
  409 | 
  410 |     const addButton = page.locator('button:has-text("新建盘点"), button:has-text("新增")')
  411 |     await expect(addButton.first()).toBeVisible({ timeout: 5000 })
  412 |   })
  413 | 
  414 |   test('路径3-步骤4: 选择物料成功', async ({ page }) => {
  415 |     await loginAs(page, 'warehouse_manager')
  416 |     await page.goto(`${FE_BASE}/stocktaking`)
  417 |     await page.waitForTimeout(1000)
  418 | 
  419 |     const addButton = page.locator('button:has-text("新建盘点"), button:has-text("新增")')
  420 |     await addButton.first().click()
  421 | 
  422 |     const materialSelect = page.locator('select[name="materialId"], [data-testid="material-select"]')
  423 |     if (await materialSelect.isVisible().catch(() => false)) {
  424 |       await materialSelect.selectOption({ index: 1 })
  425 |     }
  426 |   })
  427 | 
  428 |   test('路径3-步骤5: 查看系统库存成功', async ({ page }) => {
  429 |     if (!materialId) {
  430 |       test.skip()
  431 |       return
  432 |     }
  433 | 
  434 |     const stock = await getStock(warehouseManagerToken, materialId)
  435 |     expect(stock).toBeGreaterThanOrEqual(0)
  436 |   })
  437 | 
  438 |   test('路径3-步骤6: 填写实际库存（等于系统库存）成功', async ({ page }) => {
  439 |     if (!materialId) {
  440 |       test.skip()
  441 |       return
  442 |     }
  443 | 
  444 |     const systemStock = await getStock(warehouseManagerToken, materialId)
  445 | 
  446 |     await loginAs(page, 'warehouse_manager')
  447 |     await page.goto(`${FE_BASE}/stocktaking`)
  448 |     await page.waitForTimeout(1000)
  449 | 
  450 |     const addButton = page.locator('button:has-text("新建盘点"), button:has-text("新增")')
  451 |     await addButton.first().click()
  452 | 
  453 |     const materialSelect = page.locator('select[name="materialId"], [data-testid="material-select"]')
  454 |     if (await materialSelect.isVisible().catch(() => false)) {
  455 |       await materialSelect.selectOption({ index: 1 })
  456 |     }
  457 | 
  458 |     const actualStockInput = page.locator('input[name="actualStock"]')
  459 |     if (await actualStockInput.isVisible().catch(() => false)) {
  460 |       await actualStockInput.fill(String(systemStock))
  461 |     }
  462 |   })
  463 | 
  464 |   test('路径3-步骤7: 提交盘点成功', async ({ page }) => {
  465 |     if (!materialId) {
  466 |       test.skip()
  467 |       return
  468 |     }
  469 | 
  470 |     const systemStock = await getStock(warehouseManagerToken, materialId)
  471 | 
  472 |     const res = await apiFetch(warehouseManagerToken, 'POST', '/stocktaking', {
  473 |       materialId,
  474 |       actualStock: systemStock,
  475 |       remark: 'E2E无差异测试',
  476 |     })
  477 | 
> 478 |     expect([201, 400]).toContain(res.status)
      |                        ^ Error: expect(received).toContain(expected) // indexOf
  479 |   })
  480 | 
  481 |   test('路径3-步骤8: 确认差异成功', async ({ page }) => {
  482 |     if (!materialId) {
  483 |       test.skip()
  484 |       return
  485 |     }
  486 | 
  487 |     const systemStock = await getStock(warehouseManagerToken, materialId)
  488 | 
  489 |     const createRes = await apiFetch(warehouseManagerToken, 'POST', '/stocktaking', {
  490 |       materialId,
  491 |       actualStock: systemStock,
  492 |       remark: 'E2E无差异测试-确认差异',
  493 |     })
  494 | 
  495 |     if (createRes.status !== 201) {
  496 |       test.skip()
  497 |       return
  498 |     }
  499 | 
  500 |     const stocktakingId = createRes.data?.data?.id
  501 |     if (!stocktakingId) {
  502 |       test.skip()
  503 |       return
  504 |     }
  505 | 
  506 |     const confirmRes = await apiFetch(warehouseManagerToken, 'POST', `/stocktaking/${stocktakingId}/confirm`)
  507 |     expect([200, 400]).toContain(confirmRes.status)
  508 |   })
  509 | 
  510 |   test('路径3-步骤9: 验证库存不变', async ({ page }) => {
  511 |     if (!materialId) {
  512 |       test.skip()
  513 |       return
  514 |     }
  515 | 
  516 |     const stock = await getStock(warehouseManagerToken, materialId)
  517 |     expect(stock).toBeGreaterThanOrEqual(0)
  518 |   })
  519 | 
  520 |   test('路径3-步骤10: 验证盘点记录存在', async ({ page }) => {
  521 |     await loginAs(page, 'warehouse_manager')
  522 |     await page.goto(`${FE_BASE}/stocktaking`)
  523 |     await page.waitForTimeout(1000)
  524 | 
  525 |     const rows = page.locator('table tbody tr')
  526 |     await expect(rows.first()).toBeVisible({ timeout: 5000 })
  527 |   })
  528 | 
  529 |   test('路径3-步骤11: 验证盘点详情正确', async ({ page }) => {
  530 |     await loginAs(page, 'warehouse_manager')
  531 |     await page.goto(`${FE_BASE}/stocktaking`)
  532 |     await page.waitForTimeout(1000)
  533 | 
  534 |     const firstRow = page.locator('table tbody tr').first()
  535 |     if (await firstRow.isVisible().catch(() => false)) {
  536 |       await firstRow.click()
  537 | 
  538 |       await expect(page.locator('text=盘点详情, text=物料信息')).toBeVisible({ timeout: 5000 })
  539 |     }
  540 |   })
  541 | 
  542 |   test('路径3-步骤12: 验证库存日志正确', async ({ page }) => {
  543 |     await loginAs(page, 'warehouse_manager')
  544 |     await page.goto(`${FE_BASE}/logs`)
  545 |     await page.waitForTimeout(1000)
  546 | 
  547 |     await expect(page.locator('body')).toBeVisible()
  548 |   })
  549 | })
  550 | 
  551 | // ────────────────────────────────────────────
  552 | // 路径4: 盘点失败（物料不存在）路径
  553 | // ────────────────────────────────────────────
  554 | 
  555 | test.describe('仓管员盘点 - 盘点失败（物料不存在）路径', () => {
  556 |   test('路径4-步骤1: 仓管员登录成功', async ({ page }) => {
  557 |     await loginAs(page, 'warehouse_manager')
  558 |     await expect(page).toHaveURL(`${FE_BASE}/`)
  559 |   })
  560 | 
  561 |   test('路径4-步骤2: 进入盘点页面成功', async ({ page }) => {
  562 |     await loginAs(page, 'warehouse_manager')
  563 |     await page.goto(`${FE_BASE}/stocktaking`)
  564 |     await expect(page.locator('body')).toBeVisible({ timeout: 30000 })
  565 |   })
  566 | 
  567 |   test('路径4-步骤3: 点击新建盘点按钮成功', async ({ page }) => {
  568 |     await loginAs(page, 'warehouse_manager')
  569 |     await page.goto(`${FE_BASE}/stocktaking`)
  570 |     await page.waitForTimeout(1000)
  571 | 
  572 |     const addButton = page.locator('button:has-text("新建盘点"), button:has-text("新增")')
  573 |     await expect(addButton.first()).toBeVisible({ timeout: 5000 })
  574 |   })
  575 | 
  576 |   test('路径4-步骤4: 选择物料（不存在）失败', async ({ page }) => {
  577 |     const res = await apiFetch(warehouseManagerToken, 'POST', '/stocktaking', {
  578 |       materialId: 'non-existent-material-id',
```