# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: scenarios\technician-suite\view-projects.spec.ts >> 技术员查看项目 - 项目列表路径 >> 路径1-步骤7: 验证API返回项目列表
- Location: e2e\scenarios\technician-suite\view-projects.spec.ts:115:3

# Error details

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: 200
Received: 401
```

# Test source

```ts
  18  |   const r = await apiFetch(token, 'GET', '/projects?page=1&pageSize=1')
  19  |   return r.data?.data?.list?.[0]?.id || ''
  20  | }
  21  | 
  22  | async function getAnyBomId(token: string): Promise<string> {
  23  |   const r = await apiFetch(token, 'GET', '/boms?page=1&pageSize=1')
  24  |   return r.data?.data?.list?.[0]?.id || ''
  25  | }
  26  | 
  27  | // ────────────────────────────────────────────
  28  | // 测试数据准备
  29  | // ────────────────────────────────────────────
  30  | 
  31  | let adminToken = ''
  32  | let technicianToken = ''
  33  | let projectId = ''
  34  | let bomId = ''
  35  | 
  36  | test.beforeAll(async () => {
  37  |   adminToken = await apiLogin('admin')
  38  |   technicianToken = await apiLogin('technician')
  39  | 
  40  |   projectId = await getAnyProjectId(adminToken)
  41  |   bomId = await getAnyBomId(adminToken)
  42  | })
  43  | 
  44  | // ────────────────────────────────────────────
  45  | // 路径1: 查看项目列表 → 验证行可见 → 验证项目名称/编号列
  46  | // ────────────────────────────────────────────
  47  | 
  48  | test.describe('技术员查看项目 - 项目列表路径', () => {
  49  |   test('路径1-步骤1: 技术员登录成功', async ({ page }) => {
  50  |     await loginAs(page, 'technician')
  51  |     await expect(page).toHaveURL(`${FE_BASE}/`)
  52  |   })
  53  | 
  54  |   test('路径1-步骤2: 进入项目页面成功', async ({ page }) => {
  55  |     await loginAs(page, 'technician')
  56  |     await page.goto(`${FE_BASE}/projects`)
  57  |     await expect(page.locator('body')).toBeVisible({ timeout: 30000 })
  58  |   })
  59  | 
  60  |   test('路径1-步骤3: 验证项目列表加载', async ({ page }) => {
  61  |     await loginAs(page, 'technician')
  62  |     await page.goto(`${FE_BASE}/projects`)
  63  |     await page.waitForTimeout(1000)
  64  | 
  65  |     // 验证表格存在
  66  |     const table = page.locator('table, [role="table"], [data-testid="project-table"]')
  67  |     await expect(table.first()).toBeVisible({ timeout: 5000 })
  68  |   })
  69  | 
  70  |   test('路径1-步骤4: 验证项目列表有数据行', async ({ page }) => {
  71  |     if (!projectId) {
  72  |       test.skip()
  73  |       return
  74  |     }
  75  | 
  76  |     await loginAs(page, 'technician')
  77  |     await page.goto(`${FE_BASE}/projects`)
  78  |     await page.waitForTimeout(1000)
  79  | 
  80  |     // 验证表格有数据行
  81  |     const rows = page.locator('table tbody tr')
  82  |     await expect(rows.first()).toBeVisible({ timeout: 5000 })
  83  |   })
  84  | 
  85  |   test('路径1-步骤5: 验证项目名称列可见', async ({ page }) => {
  86  |     if (!projectId) {
  87  |       test.skip()
  88  |       return
  89  |     }
  90  | 
  91  |     await loginAs(page, 'technician')
  92  |     await page.goto(`${FE_BASE}/projects`)
  93  |     await page.waitForTimeout(1000)
  94  | 
  95  |     // 验证项目名称列存在
  96  |     const nameColumn = page.locator('th:has-text("项目名称"), th:has-text("名称"), th:has-text("Name"), th:has-text("Project")')
  97  |     await expect(nameColumn.first()).toBeVisible({ timeout: 5000 })
  98  |   })
  99  | 
  100 |   test('路径1-步骤6: 验证项目编号列可见', async ({ page }) => {
  101 |     if (!projectId) {
  102 |       test.skip()
  103 |       return
  104 |     }
  105 | 
  106 |     await loginAs(page, 'technician')
  107 |     await page.goto(`${FE_BASE}/projects`)
  108 |     await page.waitForTimeout(1000)
  109 | 
  110 |     // 验证项目编号列存在
  111 |     const codeColumn = page.locator('th:has-text("项目编号"), th:has-text("编号"), th:has-text("Code"), th:has-text("Number")')
  112 |     await expect(codeColumn.first()).toBeVisible({ timeout: 5000 })
  113 |   })
  114 | 
  115 |   test('路径1-步骤7: 验证API返回项目列表', async ({ page }) => {
  116 |     const res = await apiFetch(technicianToken, 'GET', '/projects?page=1&pageSize=10')
  117 | 
> 118 |     expect(res.status).toBe(200)
      |                        ^ Error: expect(received).toBe(expected) // Object.is equality
  119 |     expect(res.data?.data?.list).toBeDefined()
  120 |   })
  121 | })
  122 | 
  123 | // ────────────────────────────────────────────
  124 | // 路径2: 查看项目详情 → 点击第一行 → 验证详情弹窗/页面
  125 | // ────────────────────────────────────────────
  126 | 
  127 | test.describe('技术员查看项目 - 项目详情路径', () => {
  128 |   test('路径2-步骤1: 技术员登录成功', async ({ page }) => {
  129 |     await loginAs(page, 'technician')
  130 |     await expect(page).toHaveURL(`${FE_BASE}/`)
  131 |   })
  132 | 
  133 |   test('路径2-步骤2: 进入项目页面成功', async ({ page }) => {
  134 |     await loginAs(page, 'technician')
  135 |     await page.goto(`${FE_BASE}/projects`)
  136 |     await expect(page.locator('body')).toBeVisible({ timeout: 30000 })
  137 |   })
  138 | 
  139 |   test('路径2-步骤3: 验证项目列表有数据', async ({ page }) => {
  140 |     if (!projectId) {
  141 |       test.skip()
  142 |       return
  143 |     }
  144 | 
  145 |     await loginAs(page, 'technician')
  146 |     await page.goto(`${FE_BASE}/projects`)
  147 |     await page.waitForTimeout(1000)
  148 | 
  149 |     const rows = page.locator('table tbody tr')
  150 |     await expect(rows.first()).toBeVisible({ timeout: 5000 })
  151 |   })
  152 | 
  153 |   test('路径2-步骤4: 点击第一条项目记录', async ({ page }) => {
  154 |     if (!projectId) {
  155 |       test.skip()
  156 |       return
  157 |     }
  158 | 
  159 |     await loginAs(page, 'technician')
  160 |     await page.goto(`${FE_BASE}/projects`)
  161 |     await page.waitForTimeout(1000)
  162 | 
  163 |     // 点击第一条记录
  164 |     const firstRow = page.locator('table tbody tr').first()
  165 |     if (await firstRow.isVisible().catch(() => false)) {
  166 |       await firstRow.click()
  167 |     }
  168 |   })
  169 | 
  170 |   test('路径2-步骤5: 验证详情弹窗或页面显示', async ({ page }) => {
  171 |     if (!projectId) {
  172 |       test.skip()
  173 |       return
  174 |     }
  175 | 
  176 |     await loginAs(page, 'technician')
  177 |     await page.goto(`${FE_BASE}/projects`)
  178 |     await page.waitForTimeout(1000)
  179 | 
  180 |     const firstRow = page.locator('table tbody tr').first()
  181 |     if (await firstRow.isVisible().catch(() => false)) {
  182 |       await firstRow.click()
  183 |       await page.waitForTimeout(500)
  184 | 
  185 |       // 验证详情弹窗或页面显示
  186 |       const detailPanel = page.locator('text=项目详情, text=项目信息, text=Project Detail, [role="dialog"], .modal')
  187 |       await expect(detailPanel.first()).toBeVisible({ timeout: 5000 })
  188 |     }
  189 |   })
  190 | 
  191 |   test('路径2-步骤6: 验证详情包含项目名称', async ({ page }) => {
  192 |     if (!projectId) {
  193 |       test.skip()
  194 |       return
  195 |     }
  196 | 
  197 |     await loginAs(page, 'technician')
  198 |     await page.goto(`${FE_BASE}/projects`)
  199 |     await page.waitForTimeout(1000)
  200 | 
  201 |     const firstRow = page.locator('table tbody tr').first()
  202 |     if (await firstRow.isVisible().catch(() => false)) {
  203 |       await firstRow.click()
  204 |       await page.waitForTimeout(500)
  205 | 
  206 |       // 验证详情中包含项目名称字段
  207 |       const nameField = page.locator('text=项目名称, text=名称, label:has-text("名称")')
  208 |       await expect(nameField.first()).toBeVisible({ timeout: 5000 })
  209 |     }
  210 |   })
  211 | 
  212 |   test('路径2-步骤7: 验证API返回项目详情', async ({ page }) => {
  213 |     if (!projectId) {
  214 |       test.skip()
  215 |       return
  216 |     }
  217 | 
  218 |     const res = await apiFetch(technicianToken, 'GET', `/projects/${projectId}`)
```