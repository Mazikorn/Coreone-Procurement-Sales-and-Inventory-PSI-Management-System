# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: scenarios\warehouse-manager-daily-work\full-day.spec.ts >> 仓管员一天的工作 - 完整流程 >> 步骤7: 创建盘点
- Location: e2e\scenarios\warehouse-manager-daily-work\full-day.spec.ts:140:3

# Error details

```
Error: expect(received).toContain(expected) // indexOf

Expected value: 200
Received array: [201, 400]
```

# Test source

```ts
  56  | })
  57  | 
  58  | // ────────────────────────────────────────────
  59  | // 仓管员一天的工作完整流程
  60  | // ────────────────────────────────────────────
  61  | 
  62  | test.describe('仓管员一天的工作 - 完整流程', () => {
  63  |   test('步骤1: 仓管员登录', async ({ page }) => {
  64  |     await loginAs(page, 'warehouse_manager')
  65  |     await expect(page).toHaveURL(`${FE_BASE}/`)
  66  |   })
  67  | 
  68  |   test('步骤2: 查看仪表盘', async ({ page }) => {
  69  |     await loginAs(page, 'warehouse_manager')
  70  |     await page.goto(`${FE_BASE}/`)
  71  |     await page.waitForTimeout(1000)
  72  | 
  73  |     // 验证仪表盘可见
  74  |     await expect(page.locator('body')).toBeVisible()
  75  |   })
  76  | 
  77  |   test('步骤3: 处理采购入库', async ({ page }) => {
  78  |     if (!materialId || !supplierId || !locationId) {
  79  |       test.skip()
  80  |       return
  81  |     }
  82  | 
  83  |     const beforeStock = await getStock(warehouseManagerToken, materialId)
  84  | 
  85  |     const res = await apiFetch(warehouseManagerToken, 'POST', '/inbound', {
  86  |       type: 'purchase',
  87  |       materialId,
  88  |       supplierId,
  89  |       locationId,
  90  |       quantity: 100,
  91  |       batchNo: `BATCH-DAILY-${Date.now()}`,
  92  |       remark: 'E2E仓管员一天工作-采购入库',
  93  |     })
  94  | 
  95  |     expect(res.status).toBe(201)
  96  | 
  97  |     // 验证库存增加
  98  |     const afterStock = await getStock(warehouseManagerToken, materialId)
  99  |     expect(afterStock).toBeGreaterThanOrEqual(beforeStock + 100)
  100 |   })
  101 | 
  102 |   test('步骤4: 验证库存增加', async ({ page }) => {
  103 |     if (!materialId) {
  104 |       test.skip()
  105 |       return
  106 |     }
  107 | 
  108 |     const stock = await getStock(warehouseManagerToken, materialId)
  109 |     expect(stock).toBeGreaterThan(0)
  110 |   })
  111 | 
  112 |   test('步骤5: 处理直接入库', async ({ page }) => {
  113 |     if (!materialId || !locationId) {
  114 |       test.skip()
  115 |       return
  116 |     }
  117 | 
  118 |     const res = await apiFetch(warehouseManagerToken, 'POST', '/inbound', {
  119 |       type: 'direct',
  120 |       materialId,
  121 |       locationId,
  122 |       quantity: 50,
  123 |       batchNo: `BATCH-DIRECT-${Date.now()}`,
  124 |       remark: 'E2E仓管员一天工作-直接入库',
  125 |     })
  126 | 
  127 |     expect([201, 400]).toContain(res.status)
  128 |   })
  129 | 
  130 |   test('步骤6: 查看库存页面', async ({ page }) => {
  131 |     await loginAs(page, 'warehouse_manager')
  132 |     await page.goto(`${FE_BASE}/inventory`)
  133 |     await page.waitForTimeout(1000)
  134 | 
  135 |     // 验证库存列表可见
  136 |     const rows = page.locator('table tbody tr')
  137 |     await expect(rows.first()).toBeVisible({ timeout: 5000 })
  138 |   })
  139 | 
  140 |   test('步骤7: 创建盘点', async ({ page }) => {
  141 |     if (!materialId || !locationId) {
  142 |       test.skip()
  143 |       return
  144 |     }
  145 | 
  146 |     const currentStock = await getStock(warehouseManagerToken, materialId)
  147 | 
  148 |     const res = await apiFetch(warehouseManagerToken, 'POST', '/stocktaking', {
  149 |       materialId,
  150 |       locationId,
  151 |       systemStock: currentStock,
  152 |       actualStock: currentStock,
  153 |       remark: 'E2E仓管员一天工作-盘点',
  154 |     })
  155 | 
> 156 |     expect([201, 400]).toContain(res.status)
      |                        ^ Error: expect(received).toContain(expected) // indexOf
  157 |   })
  158 | 
  159 |   test('步骤8: 验证盘点记录', async ({ page }) => {
  160 |     await loginAs(page, 'warehouse_manager')
  161 |     await page.goto(`${FE_BASE}/stocktaking`)
  162 |     await page.waitForTimeout(1000)
  163 | 
  164 |     // 验证盘点记录列表可见
  165 |     await expect(page.locator('body')).toBeVisible()
  166 |   })
  167 | 
  168 |   test('步骤9: 查看预警页面', async ({ page }) => {
  169 |     await loginAs(page, 'warehouse_manager')
  170 |     await page.goto(`${FE_BASE}/alerts`)
  171 |     await page.waitForTimeout(1000)
  172 | 
  173 |     // 验证预警页面可见
  174 |     await expect(page.locator('body')).toBeVisible()
  175 |   })
  176 | 
  177 |   test('步骤10: 检查库存预警', async ({ page }) => {
  178 |     if (!materialId) {
  179 |       test.skip()
  180 |       return
  181 |     }
  182 | 
  183 |     const stock = await getStock(warehouseManagerToken, materialId)
  184 |     expect(stock).toBeGreaterThanOrEqual(0)
  185 |   })
  186 | })
  187 | 
```