# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: scenarios\cost-analysis-flow\full-flow.spec.ts >> 成本分析完整流程 >> 步骤11: 验证导出API
- Location: e2e\scenarios\cost-analysis-flow\full-flow.spec.ts:100:3

# Error details

```
Error: expect(received).toContain(expected) // indexOf

Expected value: 400
Received array: [200, 404]
```

# Test source

```ts
  2   | import { loginAs, apiLogin, apiFetch, FE_BASE, RoleKey } from '../shared/auth'
  3   | 
  4   | /**
  5   |  * 成本分析完整流程 - 场景化测试套件
  6   |  *
  7   |  * 基于"然后呢"三层追问设计：
  8   |  * 1. 场景端的然后呢：财务查看仪表盘 → 查看滑动成本 → 查看盈利 → 查看费用对比 → 查看趋势 → 导出
  9   |  * 2. 操作端的然后呢：登录 → 各页面查看 → API验证 → 导出
  10  |  * 3. 测试路径矩阵：场景树 × 操作链 × 分支条件
  11  |  */
  12  | 
  13  | // ────────────────────────────────────────────
  14  | // 测试数据准备
  15  | // ────────────────────────────────────────────
  16  | 
  17  | let adminToken = ''
  18  | let financeToken = ''
  19  | 
  20  | test.beforeAll(async () => {
  21  |   adminToken = await apiLogin('admin')
  22  |   financeToken = await apiLogin('finance')
  23  | })
  24  | 
  25  | // ────────────────────────────────────────────
  26  | // 成本分析完整流程
  27  | // ────────────────────────────────────────────
  28  | 
  29  | test.describe('成本分析完整流程', () => {
  30  |   test('步骤1: 财务登录', async ({ page }) => {
  31  |     await loginAs(page, 'finance')
  32  |     await expect(page).toHaveURL(`${FE_BASE}/`)
  33  |   })
  34  | 
  35  |   test('步骤2: 查看ABC成本仪表盘', async ({ page }) => {
  36  |     await loginAs(page, 'finance')
  37  |     await page.goto(`${FE_BASE}/abc/dashboard`)
  38  |     await page.waitForTimeout(1000)
  39  | 
  40  |     // 验证ABC仪表盘页面可见
  41  |     await expect(page.locator('body')).toBeVisible()
  42  |   })
  43  | 
  44  |   test('步骤3: 验证ABC仪表盘API', async ({ page }) => {
  45  |     const res = await apiFetch(financeToken, 'GET', '/abc/dashboard')
  46  |     expect([200, 404]).toContain(res.status)
  47  |   })
  48  | 
  49  |   test('步骤4: 查看单张切片成本页面', async ({ page }) => {
  50  |     await loginAs(page, 'finance')
  51  |     await page.goto(`${FE_BASE}/abc/slide-cost`)
  52  |     await page.waitForTimeout(1000)
  53  | 
  54  |     // 验证单张切片成本页面可见
  55  |     await expect(page.locator('body')).toBeVisible()
  56  |   })
  57  | 
  58  |   test('步骤5: 查看盈利能力页面', async ({ page }) => {
  59  |     await loginAs(page, 'finance')
  60  |     await page.goto(`${FE_BASE}/abc/profitability`)
  61  |     await page.waitForTimeout(1000)
  62  | 
  63  |     // 验证盈利能力页面可见
  64  |     await expect(page.locator('body')).toBeVisible()
  65  |   })
  66  | 
  67  |   test('步骤6: 验证盈利能力API', async ({ page }) => {
  68  |     const res = await apiFetch(financeToken, 'GET', '/abc/profitability')
  69  |     expect([200, 404]).toContain(res.status)
  70  |   })
  71  | 
  72  |   test('步骤7: 查看费用对比页面', async ({ page }) => {
  73  |     await loginAs(page, 'finance')
  74  |     await page.goto(`${FE_BASE}/abc/fee-comparison`)
  75  |     await page.waitForTimeout(1000)
  76  | 
  77  |     // 验证费用对比页面可见
  78  |     await expect(page.locator('body')).toBeVisible()
  79  |   })
  80  | 
  81  |   test('步骤8: 验证费用对比API', async ({ page }) => {
  82  |     const res = await apiFetch(financeToken, 'GET', '/abc/fee-comparison')
  83  |     expect([200, 404]).toContain(res.status)
  84  |   })
  85  | 
  86  |   test('步骤9: 查看成本趋势页面', async ({ page }) => {
  87  |     await loginAs(page, 'finance')
  88  |     await page.goto(`${FE_BASE}/abc/trend`)
  89  |     await page.waitForTimeout(1000)
  90  | 
  91  |     // 验证成本趋势页面可见
  92  |     await expect(page.locator('body')).toBeVisible()
  93  |   })
  94  | 
  95  |   test('步骤10: 验证成本趋势API', async ({ page }) => {
  96  |     const res = await apiFetch(financeToken, 'GET', '/abc/trend')
  97  |     expect([200, 404]).toContain(res.status)
  98  |   })
  99  | 
  100 |   test('步骤11: 验证导出API', async ({ page }) => {
  101 |     const res = await apiFetch(financeToken, 'GET', '/abc/export')
> 102 |     expect([200, 404]).toContain(res.status)
      |                        ^ Error: expect(received).toContain(expected) // indexOf
  103 |   })
  104 | 
  105 |   test('步骤12: 查看成本分析页面', async ({ page }) => {
  106 |     await loginAs(page, 'finance')
  107 |     await page.goto(`${FE_BASE}/cost-analysis`)
  108 |     await page.waitForTimeout(1000)
  109 | 
  110 |     // 验证成本分析页面可见
  111 |     await expect(page.locator('body')).toBeVisible()
  112 |   })
  113 | })
  114 | 
```