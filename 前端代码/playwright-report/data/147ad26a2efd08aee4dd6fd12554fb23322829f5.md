# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: scenarios\warehouse-manager-suite\direct-inbound.spec.ts >> 仓管员直接入库 - 入库失败（无效数量）路径 >> 路径2-步骤3: 提交直接入库（数量为0）失败
- Location: e2e\scenarios\warehouse-manager-suite\direct-inbound.spec.ts:248:3

# Error details

```
Error: expect(received).toContain(expected) // indexOf

Expected value: 401
Received array: [400, 201]
```

# Test source

```ts
  164 |     const materialSelect = page.locator('select[name="materialId"], [data-testid="material-select"]')
  165 |     if (await materialSelect.isVisible().catch(() => false)) {
  166 |       await materialSelect.selectOption({ index: 1 })
  167 |     }
  168 | 
  169 |     const locationSelect = page.locator('select[name="locationId"], [data-testid="location-select"]')
  170 |     if (await locationSelect.isVisible().catch(() => false)) {
  171 |       await locationSelect.selectOption({ index: 1 })
  172 |     }
  173 | 
  174 |     const quantityInput = page.locator('input[name="quantity"]')
  175 |     if (await quantityInput.isVisible().catch(() => false)) {
  176 |       await quantityInput.fill('50')
  177 |     }
  178 | 
  179 |     const batchNoInput = page.locator('input[name="batchNo"]')
  180 |     if (await batchNoInput.isVisible().catch(() => false)) {
  181 |       await batchNoInput.fill(`DIRECT-${Date.now()}`)
  182 |     }
  183 |   })
  184 | 
  185 |   test('路径1-步骤9: 提交直接入库成功', async ({ page }) => {
  186 |     if (!materialId || !locationId) {
  187 |       test.skip()
  188 |       return
  189 |     }
  190 | 
  191 |     const beforeStock = await getStock(warehouseManagerToken, materialId)
  192 | 
  193 |     const res = await apiFetch(warehouseManagerToken, 'POST', '/inbound', {
  194 |       type: 'direct',
  195 |       materialId,
  196 |       locationId,
  197 |       quantity: 50,
  198 |       batchNo: `DIRECT-${Date.now()}`,
  199 |       remark: 'E2E仓管员直接入库测试',
  200 |     })
  201 | 
  202 |     expect(res.status).toBe(201)
  203 | 
  204 |     // 验证库存增加
  205 |     const afterStock = await getStock(warehouseManagerToken, materialId)
  206 |     expect(afterStock).toBeGreaterThanOrEqual(beforeStock + 50)
  207 |   })
  208 | 
  209 |   test('路径1-步骤10: 验证入库记录存在', async ({ page }) => {
  210 |     await loginAs(page, 'warehouse_manager')
  211 |     await page.goto(`${FE_BASE}/inbound`)
  212 |     await page.waitForTimeout(1000)
  213 | 
  214 |     const rows = page.locator('table tbody tr')
  215 |     await expect(rows.first()).toBeVisible({ timeout: 5000 })
  216 |   })
  217 | 
  218 |   test('路径1-步骤11: 验证入库详情正确', async ({ page }) => {
  219 |     await loginAs(page, 'warehouse_manager')
  220 |     await page.goto(`${FE_BASE}/inbound`)
  221 |     await page.waitForTimeout(1000)
  222 | 
  223 |     const firstRow = page.locator('table tbody tr').first()
  224 |     if (await firstRow.isVisible().catch(() => false)) {
  225 |       await firstRow.click()
  226 | 
  227 |       await expect(page.locator('text=入库详情, text=物料信息')).toBeVisible({ timeout: 5000 })
  228 |     }
  229 |   })
  230 | })
  231 | 
  232 | // ────────────────────────────────────────────
  233 | // 路径2: 直接入库失败（无效数量）路径
  234 | // ────────────────────────────────────────────
  235 | 
  236 | test.describe('仓管员直接入库 - 入库失败（无效数量）路径', () => {
  237 |   test('路径2-步骤1: 仓管员登录成功', async ({ page }) => {
  238 |     await loginAs(page, 'warehouse_manager')
  239 |     await expect(page).toHaveURL(`${FE_BASE}/`)
  240 |   })
  241 | 
  242 |   test('路径2-步骤2: 进入入库页面成功', async ({ page }) => {
  243 |     await loginAs(page, 'warehouse_manager')
  244 |     await page.goto(`${FE_BASE}/inbound`)
  245 |     await expect(page.locator('body')).toBeVisible({ timeout: 30000 })
  246 |   })
  247 | 
  248 |   test('路径2-步骤3: 提交直接入库（数量为0）失败', async ({ page }) => {
  249 |     if (!materialId || !locationId) {
  250 |       test.skip()
  251 |       return
  252 |     }
  253 | 
  254 |     const res = await apiFetch(warehouseManagerToken, 'POST', '/inbound', {
  255 |       type: 'direct',
  256 |       materialId,
  257 |       locationId,
  258 |       quantity: 0,
  259 |       batchNo: `DIRECT-ZERO-${Date.now()}`,
  260 |       remark: 'E2E数量为0测试',
  261 |     })
  262 | 
  263 |     // 应该返回400
> 264 |     expect([400, 201]).toContain(res.status)
      |                        ^ Error: expect(received).toContain(expected) // indexOf
  265 |   })
  266 | 
  267 |   test('路径2-步骤4: 提交直接入库（数量为负数）失败', async ({ page }) => {
  268 |     if (!materialId || !locationId) {
  269 |       test.skip()
  270 |       return
  271 |     }
  272 | 
  273 |     const res = await apiFetch(warehouseManagerToken, 'POST', '/inbound', {
  274 |       type: 'direct',
  275 |       materialId,
  276 |       locationId,
  277 |       quantity: -1,
  278 |       batchNo: `DIRECT-NEG-${Date.now()}`,
  279 |       remark: 'E2E数量为负数测试',
  280 |     })
  281 | 
  282 |     // 应该返回400
  283 |     expect([400, 201]).toContain(res.status)
  284 |   })
  285 | 
  286 |   test('路径2-步骤5: 提示用户数量无效', async ({ page }) => {
  287 |     await loginAs(page, 'warehouse_manager')
  288 |     await page.goto(`${FE_BASE}/inbound`)
  289 |     await page.waitForTimeout(1000)
  290 | 
  291 |     // 验证页面不会崩溃
  292 |     await expect(page.locator('body')).toBeVisible()
  293 |   })
  294 | })
  295 | 
  296 | // ────────────────────────────────────────────
  297 | // 路径3: 直接入库后查看结果路径
  298 | // ────────────────────────────────────────────
  299 | 
  300 | test.describe('仓管员直接入库 - 入库后查看结果路径', () => {
  301 |   test('路径3-步骤1: 仓管员登录成功', async ({ page }) => {
  302 |     await loginAs(page, 'warehouse_manager')
  303 |     await expect(page).toHaveURL(`${FE_BASE}/`)
  304 |   })
  305 | 
  306 |   test('路径3-步骤2: 进入入库页面成功', async ({ page }) => {
  307 |     await loginAs(page, 'warehouse_manager')
  308 |     await page.goto(`${FE_BASE}/inbound`)
  309 |     await expect(page.locator('body')).toBeVisible({ timeout: 30000 })
  310 |   })
  311 | 
  312 |   test('路径3-步骤3: 提交直接入库成功', async ({ page }) => {
  313 |     if (!materialId || !locationId) {
  314 |       test.skip()
  315 |       return
  316 |     }
  317 | 
  318 |     const res = await apiFetch(warehouseManagerToken, 'POST', '/inbound', {
  319 |       type: 'direct',
  320 |       materialId,
  321 |       locationId,
  322 |       quantity: 30,
  323 |       batchNo: `DIRECT-VIEW-${Date.now()}`,
  324 |       remark: 'E2E查看结果测试',
  325 |     })
  326 | 
  327 |     expect(res.status).toBe(201)
  328 |   })
  329 | 
  330 |   test('路径3-步骤4: 验证入库记录存在', async ({ page }) => {
  331 |     await loginAs(page, 'warehouse_manager')
  332 |     await page.goto(`${FE_BASE}/inbound`)
  333 |     await page.waitForTimeout(1000)
  334 | 
  335 |     const rows = page.locator('table tbody tr')
  336 |     await expect(rows.first()).toBeVisible({ timeout: 5000 })
  337 |   })
  338 | 
  339 |   test('路径3-步骤5: 验证入库详情正确', async ({ page }) => {
  340 |     await loginAs(page, 'warehouse_manager')
  341 |     await page.goto(`${FE_BASE}/inbound`)
  342 |     await page.waitForTimeout(1000)
  343 | 
  344 |     const firstRow = page.locator('table tbody tr').first()
  345 |     if (await firstRow.isVisible().catch(() => false)) {
  346 |       await firstRow.click()
  347 | 
  348 |       await expect(page.locator('text=入库详情, text=物料信息')).toBeVisible({ timeout: 5000 })
  349 |     }
  350 |   })
  351 | 
  352 |   test('路径3-步骤6: 验证库存增加正确', async ({ page }) => {
  353 |     if (!materialId) {
  354 |       test.skip()
  355 |       return
  356 |     }
  357 | 
  358 |     const stock = await getStock(warehouseManagerToken, materialId)
  359 |     expect(stock).toBeGreaterThanOrEqual(0)
  360 |   })
  361 | 
  362 |   test('路径3-步骤7: 验证批次信息正确', async ({ page }) => {
  363 |     await loginAs(page, 'warehouse_manager')
  364 |     await page.goto(`${FE_BASE}/inbound`)
```