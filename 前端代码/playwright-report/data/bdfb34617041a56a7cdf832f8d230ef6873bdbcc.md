# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: scenarios\warehouse-manager-suite\purchase-inbound.spec.ts >> 仓管员采购入库 - 正常入库成功路径 >> 路径1-步骤10: 提交入库成功
- Location: e2e\scenarios\warehouse-manager-suite\purchase-inbound.spec.ts:256:3

# Error details

```
Error: expect(received).toBeGreaterThanOrEqual(expected)

Expected: >= 108.5
Received:    8.5
```

# Test source

```ts
  178 | 
  179 |     // 点击新增入库按钮
  180 |     const addButton = page.locator('button:has-text("新增入库"), button:has-text("新增")')
  181 |     await addButton.first().click()
  182 | 
  183 |     // 选择采购入库类型
  184 |     const purchaseType = page.locator('text=采购入库, [value="purchase"]')
  185 |     await purchaseType.first().click()
  186 | 
  187 |     // 选择物料
  188 |     const materialSelect = page.locator('select[name="materialId"], [data-testid="material-select"]')
  189 |     if (await materialSelect.isVisible().catch(() => false)) {
  190 |       await materialSelect.selectOption({ index: 1 })
  191 |     }
  192 | 
  193 |     // 选择供应商
  194 |     const supplierSelect = page.locator('select[name="supplierId"], [data-testid="supplier-select"]')
  195 |     if (await supplierSelect.isVisible().catch(() => false)) {
  196 |       await supplierSelect.selectOption({ index: 1 })
  197 |     }
  198 | 
  199 |     // 选择库位
  200 |     const locationSelect = page.locator('select[name="locationId"], [data-testid="location-select"]')
  201 |     if (await locationSelect.isVisible().catch(() => false)) {
  202 |       await locationSelect.selectOption({ index: 1 })
  203 |     }
  204 | 
  205 |     // 填写入库数量
  206 |     const quantityInput = page.locator('input[name="quantity"]')
  207 |     if (await quantityInput.isVisible().catch(() => false)) {
  208 |       await quantityInput.fill('100')
  209 |     }
  210 |   })
  211 | 
  212 |   test('路径1-步骤9: 填写批次号成功', async ({ page }) => {
  213 |     await loginAs(page, 'warehouse_manager')
  214 |     await page.goto(`${FE_BASE}/inbound`)
  215 |     await page.waitForTimeout(1000)
  216 | 
  217 |     // 点击新增入库按钮
  218 |     const addButton = page.locator('button:has-text("新增入库"), button:has-text("新增")')
  219 |     await addButton.first().click()
  220 | 
  221 |     // 选择采购入库类型
  222 |     const purchaseType = page.locator('text=采购入库, [value="purchase"]')
  223 |     await purchaseType.first().click()
  224 | 
  225 |     // 选择物料
  226 |     const materialSelect = page.locator('select[name="materialId"], [data-testid="material-select"]')
  227 |     if (await materialSelect.isVisible().catch(() => false)) {
  228 |       await materialSelect.selectOption({ index: 1 })
  229 |     }
  230 | 
  231 |     // 选择供应商
  232 |     const supplierSelect = page.locator('select[name="supplierId"], [data-testid="supplier-select"]')
  233 |     if (await supplierSelect.isVisible().catch(() => false)) {
  234 |       await supplierSelect.selectOption({ index: 1 })
  235 |     }
  236 | 
  237 |     // 选择库位
  238 |     const locationSelect = page.locator('select[name="locationId"], [data-testid="location-select"]')
  239 |     if (await locationSelect.isVisible().catch(() => false)) {
  240 |       await locationSelect.selectOption({ index: 1 })
  241 |     }
  242 | 
  243 |     // 填写入库数量
  244 |     const quantityInput = page.locator('input[name="quantity"]')
  245 |     if (await quantityInput.isVisible().catch(() => false)) {
  246 |       await quantityInput.fill('100')
  247 |     }
  248 | 
  249 |     // 填写批次号
  250 |     const batchNoInput = page.locator('input[name="batchNo"]')
  251 |     if (await batchNoInput.isVisible().catch(() => false)) {
  252 |       await batchNoInput.fill(`BATCH-${Date.now()}`)
  253 |     }
  254 |   })
  255 | 
  256 |   test('路径1-步骤10: 提交入库成功', async ({ page }) => {
  257 |     if (!materialId || !supplierId || !locationId) {
  258 |       test.skip()
  259 |       return
  260 |     }
  261 | 
  262 |     const beforeStock = await getStock(warehouseManagerToken, materialId)
  263 | 
  264 |     const res = await apiFetch(warehouseManagerToken, 'POST', '/inbound', {
  265 |       type: 'purchase',
  266 |       materialId,
  267 |       supplierId,
  268 |       locationId,
  269 |       quantity: 100,
  270 |       batchNo: `BATCH-${Date.now()}`,
  271 |       remark: 'E2E仓管员采购入库测试',
  272 |     })
  273 | 
  274 |     expect(res.status).toBe(201)
  275 | 
  276 |     // 验证库存增加
  277 |     const afterStock = await getStock(warehouseManagerToken, materialId)
> 278 |     expect(afterStock).toBeGreaterThanOrEqual(beforeStock + 100)
      |                        ^ Error: expect(received).toBeGreaterThanOrEqual(expected)
  279 |   })
  280 | 
  281 |   test('路径1-步骤11: 验证入库记录存在', async ({ page }) => {
  282 |     await loginAs(page, 'warehouse_manager')
  283 |     await page.goto(`${FE_BASE}/inbound`)
  284 |     await page.waitForTimeout(1000)
  285 | 
  286 |     // 验证入库记录存在
  287 |     const rows = page.locator('table tbody tr')
  288 |     await expect(rows.first()).toBeVisible({ timeout: 5000 })
  289 |   })
  290 | 
  291 |   test('路径1-步骤12: 验证入库详情正确', async ({ page }) => {
  292 |     await loginAs(page, 'warehouse_manager')
  293 |     await page.goto(`${FE_BASE}/inbound`)
  294 |     await page.waitForTimeout(1000)
  295 | 
  296 |     // 点击第一条记录查看详情
  297 |     const firstRow = page.locator('table tbody tr').first()
  298 |     if (await firstRow.isVisible().catch(() => false)) {
  299 |       await firstRow.click()
  300 | 
  301 |       // 验证详情页显示
  302 |       await expect(page.locator('text=入库详情, text=物料信息')).toBeVisible({ timeout: 5000 })
  303 |     }
  304 |   })
  305 | 
  306 |   test('路径1-步骤13: 验证库存增加正确', async ({ page }) => {
  307 |     if (!materialId) {
  308 |       test.skip()
  309 |       return
  310 |     }
  311 | 
  312 |     const stock = await getStock(warehouseManagerToken, materialId)
  313 |     expect(stock).toBeGreaterThanOrEqual(0)
  314 |   })
  315 | 
  316 |   test('路径1-步骤14: 验证批次信息正确', async ({ page }) => {
  317 |     await loginAs(page, 'warehouse_manager')
  318 |     await page.goto(`${FE_BASE}/inbound`)
  319 |     await page.waitForTimeout(1000)
  320 | 
  321 |     // 点击第一条记录查看详情
  322 |     const firstRow = page.locator('table tbody tr').first()
  323 |     if (await firstRow.isVisible().catch(() => false)) {
  324 |       await firstRow.click()
  325 | 
  326 |       // 验证批次信息显示
  327 |       await expect(page.locator('text=批次号, text=批次信息')).toBeVisible({ timeout: 5000 })
  328 |     }
  329 |   })
  330 | })
  331 | 
  332 | // ────────────────────────────────────────────
  333 | // 路径2: 入库失败（物料不存在）路径
  334 | // ────────────────────────────────────────────
  335 | 
  336 | test.describe('仓管员采购入库 - 入库失败（物料不存在）路径', () => {
  337 |   test('路径2-步骤1: 仓管员登录成功', async ({ page }) => {
  338 |     await loginAs(page, 'warehouse_manager')
  339 |     await expect(page).toHaveURL(`${FE_BASE}/`)
  340 |   })
  341 | 
  342 |   test('路径2-步骤2: 进入入库页面成功', async ({ page }) => {
  343 |     await loginAs(page, 'warehouse_manager')
  344 |     await page.goto(`${FE_BASE}/inbound`)
  345 |     await expect(page.locator('body')).toBeVisible({ timeout: 30000 })
  346 |   })
  347 | 
  348 |   test('路径2-步骤3: 点击新增入库按钮成功', async ({ page }) => {
  349 |     await loginAs(page, 'warehouse_manager')
  350 |     await page.goto(`${FE_BASE}/inbound`)
  351 |     await page.waitForTimeout(1000)
  352 | 
  353 |     const addButton = page.locator('button:has-text("新增入库"), button:has-text("新增")')
  354 |     await expect(addButton.first()).toBeVisible({ timeout: 5000 })
  355 |   })
  356 | 
  357 |   test('路径2-步骤4: 选择采购入库类型', async ({ page }) => {
  358 |     await loginAs(page, 'warehouse_manager')
  359 |     await page.goto(`${FE_BASE}/inbound`)
  360 |     await page.waitForTimeout(1000)
  361 | 
  362 |     const addButton = page.locator('button:has-text("新增入库"), button:has-text("新增")')
  363 |     await addButton.first().click()
  364 | 
  365 |     const purchaseType = page.locator('text=采购入库, [value="purchase"]')
  366 |     await expect(purchaseType.first()).toBeVisible({ timeout: 5000 })
  367 |   })
  368 | 
  369 |   test('路径2-步骤5: 选择物料（不存在）失败', async ({ page }) => {
  370 |     if (!supplierId || !locationId) {
  371 |       test.skip()
  372 |       return
  373 |     }
  374 | 
  375 |     const res = await apiFetch(warehouseManagerToken, 'POST', '/inbound', {
  376 |       type: 'purchase',
  377 |       materialId: 'non-existent-material-id',
  378 |       supplierId,
```